# alpha-proto

> Neutral config folder. Internal use only.

## Memory file
- `zp_memory.json` — independent persistent memory snapshot.
  - Keep repo **private**.
  - Optional: encrypt `zp_memory.json` with GPG (see `../ZP_GPG_NOTE.txt`).

## Chat usage (ru)
- Скажи в чате: «Подгрузи память из `core-config/alpha-proto/zp_memory.json`».
- Для сохранения: «Сохрани в память: ...» — файл будет обновлён и закоммичен.